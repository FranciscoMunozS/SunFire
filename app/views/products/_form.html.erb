
<%= simple_form_for(@product) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :code, label: 'Codigo', disabled:true %>
    <%= f.input :name, label: 'Nombre Producto' %>
    <%= f.input :state, label: 'Estado producto' %>
    <%= f.input :quantity, label: 'Cantidad' %>
    <%= f.input :ubication, label: 'Ubicación' %>
    <%= f.input :income_date, label: 'Fecha Ingreso', as: :string %>
    <%= f.input :bill, label: 'Factura' %>
    <%= f.input :provider, label: 'Proveedor' %>
    <%= f.input :unit_value, label: 'Valor Unitario' %>


  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
<% end %>
